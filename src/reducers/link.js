import initialState from './initialState';

export default function fuelSavingsReducer(state = initialState.link, action) {
  let newState;

  switch (action.type) {
    default:
      return state;
  }
}
